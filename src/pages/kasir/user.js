import {React,useState} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StyleSheet,
  Text,
  View,
  Image,
  TextInput,
  Pressable,
  Alert,
} from 'react-native';

function User() {

    return(
        <SafeAreaView style={styles.container}>
            <ScrollView>
                <Text>Nice</Text>
            </ScrollView>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({
});

export default User;